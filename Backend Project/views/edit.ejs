<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
</head>
</head>
<body>
    <div class="w-full min-h-screen bg-zinc-900 text-white p-10">
        <div class="w-full flex justify-end">
            <a href="/logout" class="text-sm mb-5 px-5 bg-red-500 rounded-md py-2 text-white inline-block">Logout</a>
        </div>
   
    <h5 class="mb-5">Edit your post!</h5>
        <form action="/update/<%= post._id%>" method="POST">
            <textarea class="w-1/3 p-3 block outline-none resize-none bg-transparent border-2 border-zinc-800 rounded-md" placeholder="Whats on your mind!" name="content"><%= post.content %></textarea>
            <input type="submit" value="Update your post" class="px-3 py-2 bg-yellow-500 text-black block rounded-md mt-2 w-40 text-sm">
        </form>
    
</div>
</body>
</html>