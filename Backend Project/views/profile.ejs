<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
</head>
</head>
<body>
    <div class="w-full min-h-screen bg-zinc-900 text-white p-10">
        <div class="w-full flex justify-end">
            <a href="/logout" class="text-sm mb-5 px-5 bg-red-500 rounded-md py-2 text-white inline-block">Logout</a>
        </div>
    <h3 class="text-3xl mb-2">Hello <%= user.name%>!</h3>
    <h5 class="mb-5">You can create a post!</h5>
        <form action="/posts" method="POST">
            <textarea class="w-1/3 p-3 block outline-none resize-none bg-transparent border-2 border-zinc-800 rounded-md" placeholder="Whats on your mind!" name="content"></textarea>
            <input type="submit" value="Create a new post" class="px-3 py-2 bg-blue-500 block rounded-md mt-2 w-40 text-sm">
        </form>
    
    <div class="posts mt-20">
        <h3 class="text-zinc-400">Your Posts!</h3>
        <div class="post-container mt-5">
            <% user.posts.reverse().forEach(function(post){%>
                <div class="post mb-4 w-1/3 p-4 border-[1px] border-zinc-700 rounded-md bg-zinc-800">
                    <h4 class="text-blue-500 mb-2 ">@<%= user.username%></h4>
                    <p class="text-sm tracking-tight"><%= post.content %></p>
                    <small class="mt-2 inline-block"><%= post.likes.length%></small>
                    <div class="btns flex gap-4 mt-1">
                        <a href="/like/<%= post._id %>" class="text-blue-500">
                            <%= post.likes.indexOf(user._id)=== -1 ? "Like" : "Unlike" %>
                        </a>
                        <a href="/edit/<%= post._id %>" class="text-zinc-600">edit</a>
                    </div>
                </div>
            <%})%>
        </div>
    </div>
</div>
</body>
</html>